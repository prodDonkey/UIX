# Zellij 开发布局

## 一键启动
在项目根目录执行：

```bash
zellij --layout .zellij/layout.kdl
```

会自动打开 4 个 tab：
1. `android-playground`
2. `后端`
3. `前端`
4. `任务服务`

## 前置条件
- 已安装 `zellij`
- 已安装并可用：`uv`、`npm`、`npx`
- adb 设备可连接（Android Playground 需要）
